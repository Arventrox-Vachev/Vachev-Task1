import styled from "styled-components";
import { SectionContainer as _SectionContainer, Image as _Image } from "components";

export const SectionContainer = styled(_SectionContainer)`
  padding-top: 30px;
`;

export const Image = styled(_Image)``;
